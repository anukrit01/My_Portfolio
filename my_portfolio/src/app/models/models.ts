export interface Education {
    institute: string;
    course: string;
    duration: string;
    score: string;
}

export interface Experience {
    role: string;
    company: string;
    duration: string;
    desc: string[];
}

export interface Skill {
    name: string;
    level: string;
    rating: number;
}
